---
phase: 02-app
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dekei1/src/app/layout.tsx
  - dekei1/src/app/globals.css
  - dekei1/src/app/page.tsx
  - dekei1/src/types/index.ts
  - dekei1/public/logo-ltc-de-kei.png
autonomous: true
requirements: [MATCH-01, MATCH-02, MATCH-03, DSGN-01, DSGN-02, DSGN-03, UX-01, UX-04]

must_haves:
  truths:
    - "App toont LTC de Kei header met logo en donkerblauwe achtergrond (#1B3A5C)"
    - "Alle 6 wedstrijden zijn zichtbaar met ronde, datum, tegenstander, thuis/uit badge en aanvangstijd"
    - "Thuis/uit is visueel duidelijk onderscheiden — Thuis = blauw badge, Uit = oranje badge"
    - "Tegenstander en tijd zijn prominent zichtbaar per wedstrijd"
    - "Design werkt prettig op telefoon (touch-friendly, grote klikbare elementen)"
    - "Oranje accent (#E87722) is zichtbaar als designelement"
    - "Tennisbal/padel element is aanwezig als design accent"
  artifacts:
    - path: "dekei1/src/types/index.ts"
      provides: "Match en Availability TypeScript types"
      exports: ["Match", "Availability", "PlayerName", "AvailabilityStatus"]
    - path: "dekei1/src/app/page.tsx"
      provides: "Server Component die matches + availability laadt en MatchCard componenten rendert"
      min_lines: 60
    - path: "dekei1/src/app/layout.tsx"
      provides: "LTC de Kei branding shell met metadata en globale stijlen"
    - path: "dekei1/public/logo-ltc-de-kei.png"
      provides: "LTC de Kei logo voor gebruik in header"
  key_links:
    - from: "dekei1/src/app/page.tsx"
      to: "supabase.from('dekei1_matches')"
      via: "Server Component met await"
      pattern: "from\\('dekei1_matches'\\)"
    - from: "dekei1/src/app/page.tsx"
      to: "dekei1/src/types/index.ts"
      via: "TypeScript import"
      pattern: "import.*Match.*from.*types"
---

<objective>
Bouw de visuele shell van de app: LTC de Kei huisstijl, header met logo, en het wedstrijdoverzicht.

Purpose: De basis van de app staan — matches worden getoond in de juiste huisstijl. Plan 02 bouwt hierop verder met interactiviteit.
Output: Volledig gestylede single-page app met alle 6 wedstrijden correct weergegeven, logo zichtbaar, LTC de Kei kleuren.
</objective>

<execution_context>
@/Users/michelhelsdingen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michelhelsdingen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Bestaande Supabase client — gebruik direct, geen wijzigingen nodig -->

From dekei1/src/lib/supabase.ts:
```typescript
import { createClient } from '@supabase/supabase-js'
export const supabase = createClient(supabaseUrl, supabaseAnonKey)
// Gebruik: supabase.from('dekei1_matches'), supabase.from('dekei1_availability')
```

Database schema (uit Phase 1):
```sql
-- public.dekei1_matches
id          uuid PRIMARY KEY
round       integer    -- 1-6
match_date  text       -- "za 4-4-2026"
match_time  text       -- "09:30"
opponent    text       -- "TEZ 3"
home_away   text       -- "home" of "away"

-- public.dekei1_availability
id            uuid PRIMARY KEY
match_id      uuid REFERENCES dekei1_matches(id)
player_name   text       -- "Mark", "Coen", etc.
status        text       -- "ja", "nee", "reserve"
updated_at    timestamptz
UNIQUE(match_id, player_name)
```

Logo locatie (moet gekopieerd worden naar public/):
/Users/michelhelsdingen/Desktop/Huisstijl LTC en OG/LTC/Huisstijl/Huisstijl onderdelen/briefpapier/briefpapier ltc de kei achtergrond _01.png
</interfaces>

<design>
LTC de Kei Huisstijl:
- Primair donkerblauw: #1B3A5C
- Accent oranje: #E87722
- Wit: #FFFFFF
- Lettertype: Geist (al aanwezig in layout)

Header: Donkerblauwe achtergrond, logo links of gecentreerd, "DE KEI 1" tekst, subtitel "Competitie 2026"
Wedstrijd card: Witte achtergrond, subtiele schaduw, ronde badge (blauw), tegenstander prominent, datum/tijd klein, thuis/uit badge groot genoeg om te zien

Thuis badge: donkerblauw (#1B3A5C) achtergrond, witte tekst "THUIS"
Uit badge: oranje (#E87722) achtergrond, witte tekst "UIT"

Tennisbal accent: gebruik SVG tennisbal of Unicode ⚽ als placeholder, of een CSS-gebouwde cirkel met lijnen als decoratief element.

Tailwind v4 (al aanwezig): gebruik inline styles voor LTC-specifieke kleuren die niet in default palette zitten.
</design>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types definiëren en logo kopiëren</name>
  <files>
    dekei1/src/types/index.ts
    dekei1/public/logo-ltc-de-kei.png
  </files>
  <action>
    1. Maak `dekei1/src/types/index.ts` aan met de volgende TypeScript types:

    ```typescript
    export type PlayerName = 'Mark' | 'Coen' | 'Bas' | 'Maarten' | 'Niels' | 'Michel'
    export type AvailabilityStatus = 'ja' | 'nee' | 'reserve' | null

    export interface Match {
      id: string
      round: number
      match_date: string   // "za 4-4-2026"
      match_time: string   // "09:30"
      opponent: string
      home_away: 'home' | 'away'
    }

    export interface Availability {
      id: string
      match_id: string
      player_name: PlayerName
      status: AvailabilityStatus
      updated_at: string
    }

    export interface MatchWithAvailability extends Match {
      availability: Partial<Record<PlayerName, AvailabilityStatus>>
    }

    export const PLAYERS: PlayerName[] = ['Mark', 'Coen', 'Bas', 'Maarten', 'Niels', 'Michel']
    ```

    2. Kopieer het briefpapier PNG als logo-placeholder naar `dekei1/public/logo-ltc-de-kei.png`:
    ```bash
    cp "/Users/michelhelsdingen/Desktop/Huisstijl LTC en OG/LTC/Huisstijl/Huisstijl onderdelen/briefpapier/briefpapier ltc de kei achtergrond _01.png" "/Users/michelhelsdingen/Documents/dekei1/dekei1/public/logo-ltc-de-kei.png"
    ```
    Als het kopiëren mislukt (bestand niet gevonden), maak dan een eenvoudige SVG text-logo als inline SVG in de layout — geen extern bestand nodig.
  </action>
  <verify>
    File `dekei1/src/types/index.ts` bestaat en exporteert `Match`, `Availability`, `PLAYERS`.
    Run: `cd /Users/michelhelsdingen/Documents/dekei1/dekei1 && npx tsc --noEmit 2>&1 | head -20` — geen TypeScript errors.
  </verify>
  <done>Types file bestaat met alle exports. npx tsc --noEmit slaagt zonder errors.</done>
</task>

<task type="auto">
  <name>Task 2: LTC de Kei design shell — layout en globals</name>
  <files>
    dekei1/src/app/layout.tsx
    dekei1/src/app/globals.css
  </files>
  <action>
    Vervang `dekei1/src/app/layout.tsx` volledig:
    - Metadata: title="DE KEI 1 — Beschikbaarheid", description="LTC de Kei competitie 2026 beschikbaarheid"
    - viewport meta voor mobile: width=device-width, initial-scale=1
    - Behoud Geist font imports
    - Body: witte achtergrond, min-h-screen
    - Geen andere wijzigingen in de children structuur

    Vervang `dekei1/src/app/globals.css` volledig:
    ```css
    @import "tailwindcss";

    :root {
      --ltc-blue: #1B3A5C;
      --ltc-orange: #E87722;
    }

    body {
      background-color: #f5f5f5;
      font-family: var(--font-geist-sans), Arial, sans-serif;
      -webkit-tap-highlight-color: transparent;
    }

    /* Touch-friendly tap areas */
    button {
      -webkit-tap-highlight-color: transparent;
      touch-action: manipulation;
    }
    ```

    Verwijder dark mode media query — app heeft geen dark mode.
  </action>
  <verify>
    `cd /Users/michelhelsdingen/Documents/dekei1/dekei1 && npm run build 2>&1 | tail -20` — build slaagt.
  </verify>
  <done>Build slaagt. layout.tsx heeft correcte metadata. globals.css heeft LTC kleuren als CSS variables.</done>
</task>

<task type="auto">
  <name>Task 3: Wedstrijdoverzicht — volledig gestylede pagina</name>
  <files>
    dekei1/src/app/page.tsx
  </files>
  <action>
    Vervang `dekei1/src/app/page.tsx` volledig. Dit is een Server Component (geen 'use client').

    De pagina:
    1. Haalt alle matches op: `supabase.from('dekei1_matches').select('*').order('round', { ascending: true })`
    2. Haalt ALLE availability op: `supabase.from('dekei1_availability').select('*')`
    3. Combineert data: per match een `MatchWithAvailability` object met de availability map
    4. Rendert de volledige pagina

    Layout structuur:
    ```
    <div> (grijze achtergrond, min-h-screen)
      <header> (donkerblauw #1B3A5C)
        - Logo img of SVG links
        - "DE KEI 1" titel (wit, bold)
        - "Beschikbaarheid 2026" subtitel (oranje)
        - Tennisbal decoratief element rechts (SVG cirkel met lijnen, wit/oranje)
      </header>

      <main> (max-w-lg mx-auto p-4)
        {matches.map(match => (
          <MatchCard key={match.id} match={match} />
        ))}
      </main>
    </div>
    ```

    MatchCard (inline component of apart in hetzelfde bestand, GEEN aparte file):
    ```
    <div> (witte achtergrond, rounded-xl, shadow-sm, mb-4, overflow-hidden)
      <div> (header rij, flex, items-center, p-4, border-b border-gray-100)
        <span> ronde badge (donkerblauw achtergrond, wit, "R{round}", rounded-full, klein)
        <div> (flex-1, ml-3)
          <div> (tegenstander: font-bold, text-gray-900, text-lg)
          <div> (datum + tijd: text-sm, text-gray-500)
        </div>
        <span> thuis/uit badge:
          - "THUIS": bg #1B3A5C, wit, px-3 py-1, rounded-full, text-xs font-bold
          - "UIT": bg #E87722, wit, px-3 py-1, rounded-full, text-xs font-bold
      </div>

      <div> (spelers grid sectie, p-4, bg-gray-50)
        <p> "Spelers" label (text-xs, uppercase, text-gray-400, mb-3)
        {PLAYERS.map(player => (
          <div> (speler rij, flex, items-center, justify-between, py-2)
            <span> player naam (text-sm, font-medium)
            <StatusButtons> (placeholder — wordt ingevuld in Plan 02)
              drie knoppen Ja / Nee / Reserve als disabled/grijs (skeleton state)
            </StatusButtons>
          </div>
        ))}

        <div> (teller onderaan, mt-3, pt-3, border-t border-gray-200)
          <span> "X van 6 beschikbaar" (text-sm, oranje kleur #E87722, font-semibold)
          (bereken uit availability.ja count voor deze match)
        </div>
      </div>
    </div>
    ```

    BELANGRIJKE NOOT over status knoppen placeholder:
    De drie status knoppen (Ja/Nee/Reserve) worden DISABLED gerenderd in Plan 01, want interactiviteit (Client Component) komt in Plan 02. Render ze als `<button disabled>` met grijs uiterlijk. Dit zorgt dat het design al klopt zonder de interactiviteitslogica te vermengen.

    Import types: `import type { Match, Availability, MatchWithAvailability, PlayerName } from '@/types'`
    Import PLAYERS: `import { PLAYERS } from '@/types'`
    Import supabase: `import { supabase } from '@/lib/supabase'`

    Gebruik inline styles voor LTC-specifieke kleuren (bijv. `style={{ backgroundColor: '#1B3A5C' }}`).
    Tailwind utility classes voor spacing, typography, layout.
  </action>
  <verify>
    `cd /Users/michelhelsdingen/Documents/dekei1/dekei1 && npm run build 2>&1 | tail -20` — build slaagt zonder errors.
    Visueel: `npm run dev` starten, http://localhost:3000 openen — header blauw, 6 wedstrijd cards zichtbaar, thuis/uit badges aanwezig.
  </verify>
  <done>
    Build slaagt. Pagina toont:
    - Donkerblauwe header met "DE KEI 1" en oranje subtitle
    - 6 wedstrijd cards met ronde badge, tegenstander (bold), datum+tijd, thuis/uit badge
    - Thuis = blauw badge, Uit = oranje badge
    - Spelers sectie per kaart met disabled knoppen als placeholder
    - "X van 6 beschikbaar" teller (0 initieel)
  </done>
</task>

</tasks>

<verification>
Na alle taken:
1. `cd /Users/michelhelsdingen/Documents/dekei1/dekei1 && npm run build` — slaagt zonder errors
2. `npx tsc --noEmit` — geen TypeScript errors
3. Pagina toont 6 wedstrijden in LTC de Kei huisstijl
4. Thuis/uit badges onderscheiden visueel
5. Logo of SVG logo-element aanwezig in header
</verification>

<success_criteria>
- LTC de Kei donkerblauwe header (#1B3A5C) aanwezig
- Oranje accent (#E87722) zichtbaar (badges, subtitel)
- Alle 6 wedstrijden getoond met ronde, datum, tegenstander, thuis/uit badge, aanvangstijd
- Thuis = blauw badge, Uit = oranje badge (MATCH-02)
- Tegenstander bold en prominent (MATCH-03)
- Build slaagt, TypeScript errors = 0
- TypeScript types file aangemaakt (fundament voor Plan 02)
</success_criteria>

<output>
Na completion, maak `.planning/phases/02-app/02-01-SUMMARY.md`
</output>
