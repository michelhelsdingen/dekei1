---
phase: 02-app
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - dekei1/src/app/page.tsx
  - dekei1/src/components/MatchCard.tsx
  - dekei1/src/components/PlayerSelector.tsx
  - dekei1/src/components/StatusButtons.tsx
  - dekei1/src/app/api/availability/route.ts
autonomous: true
requirements: [AVAIL-01, AVAIL-02, AVAIL-03, AVAIL-04, UX-02, UX-03, BACK-02]

must_haves:
  truths:
    - "Speler klikt zijn naam bovenaan — daarna kan hij zijn status wijzigen per wedstrijd"
    - "Status opties zijn Ja / Nee / Reserve — klik = direct opgeslagen naar Supabase, geen submit-knop"
    - "Na opslaan: visuele bevestiging (kort groen flash of check animatie op de gekozen knop)"
    - "Actieve status per speler is duidelijk zichtbaar met kleur en icon — Ja = groen ✓, Nee = rood ✕, Reserve = geel ⏱"
    - "Per wedstrijd staat een teller: 'X van 6 beschikbaar' — wordt live bijgewerkt"
    - "Klikken op andermans naam in de spelers rij heeft GEEN effect — alleen jouw eigen rij reageert op klikken"
    - "Als geen speler geselecteerd is, zijn alle status knoppen disabled — UX blokkeert onbedoeld klikken"
  artifacts:
    - path: "dekei1/src/components/MatchCard.tsx"
      provides: "Client Component die een match met availability toont en interactiviteit afhandelt"
      exports: ["MatchCard"]
      min_lines: 80
    - path: "dekei1/src/components/StatusButtons.tsx"
      provides: "Ja/Nee/Reserve knoppen met animatie en actieve staat"
      exports: ["StatusButtons"]
    - path: "dekei1/src/components/PlayerSelector.tsx"
      provides: "Bovenaan de pagina: 'Ik ben [naam]' selector"
      exports: ["PlayerSelector"]
    - path: "dekei1/src/app/api/availability/route.ts"
      provides: "POST endpoint voor upsert van availability status"
      exports: ["POST"]
    - path: "dekei1/src/app/page.tsx"
      provides: "Server Component die data laadt en Client Components rendert"
  key_links:
    - from: "dekei1/src/components/StatusButtons.tsx"
      to: "dekei1/src/app/api/availability/route.ts"
      via: "fetch POST /api/availability"
      pattern: "fetch.*api/availability"
    - from: "dekei1/src/app/api/availability/route.ts"
      to: "supabase.from('dekei1_availability')"
      via: "upsert met onConflict: match_id,player_name"
      pattern: "from\\('dekei1_availability'\\).*upsert"
    - from: "dekei1/src/app/page.tsx"
      to: "dekei1/src/components/MatchCard.tsx"
      via: "props: match data + availability map"
      pattern: "MatchCard.*match=\\{"
---

<objective>
Voeg volledige beschikbaarheidsinteractiviteit toe — speler selecteert zichzelf, klikt Ja/Nee/Reserve, wordt direct opgeslagen.

Purpose: Dit is de kernfunctionaliteit van de app. Na Plan 01 is het design klaar; nu wordt het bruikbaar.
Output: Volledig werkende app waarbij spelers hun beschikbaarheid kunnen invullen en direct zien van anderen.
</objective>

<execution_context>
@/Users/michelhelsdingen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michelhelsdingen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/02-app/02-01-SUMMARY.md

<interfaces>
<!-- Types aangemaakt in Plan 01 -->

From dekei1/src/types/index.ts:
```typescript
export type PlayerName = 'Mark' | 'Coen' | 'Bas' | 'Maarten' | 'Niels' | 'Michel'
export type AvailabilityStatus = 'ja' | 'nee' | 'reserve' | null

export interface Match {
  id: string
  round: number
  match_date: string
  match_time: string
  opponent: string
  home_away: 'home' | 'away'
}

export interface Availability {
  id: string
  match_id: string
  player_name: PlayerName
  status: AvailabilityStatus
  updated_at: string
}

export interface MatchWithAvailability extends Match {
  availability: Partial<Record<PlayerName, AvailabilityStatus>>
}

export const PLAYERS: PlayerName[] = ['Mark', 'Coen', 'Bas', 'Maarten', 'Niels', 'Michel']
```

Database UNIQUE constraint:
```sql
UNIQUE(match_id, player_name)
-- Gebruik supabase upsert met onConflict: 'match_id,player_name'
```

Supabase client:
```typescript
import { supabase } from '@/lib/supabase'
// supabase.from('dekei1_availability').upsert({...}, { onConflict: 'match_id,player_name' })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API route voor availability upsert</name>
  <files>
    dekei1/src/app/api/availability/route.ts
  </files>
  <action>
    Maak `dekei1/src/app/api/availability/route.ts` aan. Dit is een Next.js Route Handler.

    ```typescript
    import { NextResponse } from 'next/server'
    import { supabase } from '@/lib/supabase'
    import type { PlayerName, AvailabilityStatus } from '@/types'

    export async function POST(request: Request) {
      const body = await request.json()
      const { match_id, player_name, status } = body as {
        match_id: string
        player_name: PlayerName
        status: AvailabilityStatus
      }

      // Validatie
      if (!match_id || !player_name || !status) {
        return NextResponse.json({ error: 'Missing required fields' }, { status: 400 })
      }

      const validPlayers: PlayerName[] = ['Mark', 'Coen', 'Bas', 'Maarten', 'Niels', 'Michel']
      const validStatuses: AvailabilityStatus[] = ['ja', 'nee', 'reserve']

      if (!validPlayers.includes(player_name) || !validStatuses.includes(status)) {
        return NextResponse.json({ error: 'Invalid player or status' }, { status: 400 })
      }

      const { data, error } = await supabase
        .from('dekei1_availability')
        .upsert(
          {
            match_id,
            player_name,
            status,
            updated_at: new Date().toISOString(),
          },
          { onConflict: 'match_id,player_name' }
        )
        .select()
        .single()

      if (error) {
        console.error('Supabase upsert error:', error)
        return NextResponse.json({ error: error.message }, { status: 500 })
      }

      return NextResponse.json({ success: true, data })
    }
    ```

    Gebruik server-side supabase client (de bestaande supabase in lib/supabase.ts gebruikt de ANON key — dit werkt als de Supabase RLS policies het toestaan, of als RLS uit staat voor deze tabellen).
    Als NEXT_PUBLIC_SUPABASE_SERVICE_ROLE_KEY beschikbaar is in .env.local, gebruik die dan voor de API route voor zekere schrijfrechten. Check .env.local first: `cat /Users/michelhelsdingen/Documents/dekei1/dekei1/.env.local`
  </action>
  <verify>
    `cd /Users/michelhelsdingen/Documents/dekei1/dekei1 && npm run build 2>&1 | tail -10` — geen build errors op de route.
    Test: `curl -X POST http://localhost:3000/api/availability -H "Content-Type: application/json" -d '{"match_id":"test","player_name":"Mark","status":"ja"}' 2>&1` — retourneert JSON (200 of verwachte fout als match_id niet bestaat).
  </verify>
  <done>API route bestaat. Build slaagt. Route accepteert POST met match_id, player_name, status en voert upsert uit naar Supabase.</done>
</task>

<task type="auto">
  <name>Task 2: Client Components — PlayerSelector, StatusButtons, MatchCard</name>
  <files>
    dekei1/src/components/PlayerSelector.tsx
    dekei1/src/components/StatusButtons.tsx
    dekei1/src/components/MatchCard.tsx
  </files>
  <action>
    Maak de `dekei1/src/components/` directory aan en drie Client Component bestanden.

    **1. `dekei1/src/components/PlayerSelector.tsx`**

    Een component die bovenaan de pagina staat: "Wie ben jij?" met 6 naam-knoppen.
    Slaat de keuze op in localStorage (key: "dekei1_player") zodat de keuze bewaard blijft bij terugkeer.

    ```typescript
    'use client'
    import { useState, useEffect } from 'react'
    import { PLAYERS } from '@/types'
    import type { PlayerName } from '@/types'

    interface PlayerSelectorProps {
      onPlayerSelect: (player: PlayerName | null) => void
      selectedPlayer: PlayerName | null
    }

    export function PlayerSelector({ onPlayerSelect, selectedPlayer }: PlayerSelectorProps) {
      // Rendt een horizontale rij knoppen: "Ik ben:" + 6 naam-knoppen
      // Actieve knop: donkerblauw achtergrond (#1B3A5C), wit text, checkmark voor naam
      // Inactieve knop: witte achtergrond, blauwe border, blauwe tekst
      // Klikken op al-geselecteerde naam: deselecteert (toggle off)
      // Opslaan in localStorage bij keuze
    }
    ```

    Volledige implementatie:
    - Mount effect: lees localStorage, roep onPlayerSelect aan met opgeslagen waarde
    - Bij klik op naam: sla op in localStorage, roep onPlayerSelect aan
    - Styling: sticky positie (sticky top-0 z-10), witte achtergrond, schaduwtje, padding, scroll-safe op mobiel
    - Label "Ik ben:" in grijze kleine tekst boven de knoppen
    - Knoppen horizontaal scrollable op small screens (flex overflow-x-auto)

    **2. `dekei1/src/components/StatusButtons.tsx`**

    De drie knoppen Ja/Nee/Reserve per speler per wedstrijd.

    ```typescript
    'use client'
    import { useState } from 'react'
    import type { AvailabilityStatus } from '@/types'

    interface StatusButtonsProps {
      matchId: string
      playerName: string
      currentStatus: AvailabilityStatus
      isOwn: boolean  // true als dit de eigen rij is van de geselecteerde speler
    }
    ```

    Gedrag:
    - `isOwn === false`: Knoppen zijn volledig disabled, grijze stijl, geen pointer events. GEEN klikfeedback.
    - `isOwn === true`: Knoppen zijn klikbaar, tonen huidige status, animeren bij wijziging

    Status weergave (isOwn === false — andere spelers):
    - Toon ALLEEN de huidige status als een readonly badge (klein, 1 badge):
      - 'ja': groen badge met ✓ tekst "Ja"
      - 'nee': rood badge met ✕ tekst "Nee"
      - 'reserve': geel/amber badge met ⏱ tekst "Reserve"
      - null: grijze badge "—" (nog niet ingevuld)

    Status knoppen (isOwn === true — eigen rij):
    - Drie knoppen naast elkaar: [🎾 Ja] [✕ Nee] [⏳ Reserve]
    - Actieve knop: volle kleur, geselecteerd uiterlijk
      - Ja actief: groene achtergrond #16a34a, wit
      - Nee actief: rode achtergrond #dc2626, wit
      - Reserve actief: amber achtergrond #d97706, wit
    - Inactieve knoppen: witte achtergrond, gekleurde border, gekleurde tekst
    - Bij klik: fetch POST /api/availability, optimistic update (status direct tonen), dan server bevestigt
    - Succes animatie: knop flitst kort (scale-95 → scale-100 transition, of een groen checkmark overlay voor 800ms)
    - Loading state: spinner of disabled tijdens fetch (prevent double-click)

    ```typescript
    const handleStatusClick = async (newStatus: AvailabilityStatus) => {
      if (!isOwn || saving) return
      setSaving(true)
      const previousStatus = optimisticStatus
      setOptimisticStatus(newStatus)  // optimistic
      setShowSuccess(false)

      try {
        const res = await fetch('/api/availability', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ match_id: matchId, player_name: playerName, status: newStatus })
        })
        if (!res.ok) throw new Error('Save failed')
        setShowSuccess(true)
        setTimeout(() => setShowSuccess(false), 1000)
      } catch {
        setOptimisticStatus(previousStatus)  // rollback
        // Toon error state kort
      } finally {
        setSaving(false)
      }
    }
    ```

    Success feedback: een checkmark (✓) overlay of border-flash (groene border 1 seconde) op de gekozen knop.

    **3. `dekei1/src/components/MatchCard.tsx`**

    Combineert de match display met speler rijen en status knoppen.

    ```typescript
    'use client'
    import type { MatchWithAvailability, PlayerName, AvailabilityStatus } from '@/types'
    import { PLAYERS } from '@/types'
    import { StatusButtons } from './StatusButtons'

    interface MatchCardProps {
      match: MatchWithAvailability
      selectedPlayer: PlayerName | null
    }
    ```

    Layout (overneemt de design uit Plan 01, nu als Client Component):
    - Dezelfde card structuur als het Plan 01 placeholder design
    - Spelers rijen: naam links, StatusButtons rechts
    - Eigen rij (match.player_name === selectedPlayer): licht blauw achtergrond (bg-blue-50) als highlight
    - Andere rijen: normale achtergrond — GEEN klikhandler, GEEN hover effect — StatusButtons handelt isOwn=false af
    - Teller: tel `Object.values(match.availability).filter(s => s === 'ja').length` voor "X van 6 beschikbaar"

    Beschikbaarheidsteller styling: oranje tekst (#E87722), bold, onderaan de card.
  </action>
  <verify>
    `cd /Users/michelhelsdingen/Documents/dekei1/dekei1 && npm run build 2>&1 | tail -15` — build slaagt zonder TypeScript errors.
  </verify>
  <done>
    Drie component bestanden bestaan. Build slaagt.
    Components: PlayerSelector exporteert PlayerSelector, StatusButtons exporteert StatusButtons, MatchCard exporteert MatchCard.
    TypeScript strict mode errors = 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: Pagina samenstellen — Server Component + Client Components koppelen</name>
  <files>
    dekei1/src/app/page.tsx
  </files>
  <action>
    Vervang `dekei1/src/app/page.tsx` volledig. Dit blijft een Server Component die data laadt en dan Client Components rendert.

    Architectuur: Server Component laadt data → geeft als props aan een Client wrapper component.

    Maak een `ClientApp` component INLINE in page.tsx (of importeer uit components) met 'use client' die de selectedPlayer state beheert en alle MatchCards rendert.

    Patroon:
    ```typescript
    // page.tsx (Server Component)
    import { supabase } from '@/lib/supabase'
    import type { MatchWithAvailability, PlayerName, AvailabilityStatus } from '@/types'
    import { PLAYERS } from '@/types'
    import { AppClient } from '@/components/AppClient'

    export default async function Home() {
      // Laad matches
      const { data: matches } = await supabase
        .from('dekei1_matches')
        .select('*')
        .order('round', { ascending: true })

      // Laad alle availability
      const { data: availability } = await supabase
        .from('dekei1_availability')
        .select('*')

      // Combineer: per match een availability map
      const matchesWithAvailability: MatchWithAvailability[] = (matches ?? []).map(match => {
        const matchAvailability = (availability ?? []).filter(a => a.match_id === match.id)
        const availabilityMap: Partial<Record<PlayerName, AvailabilityStatus>> = {}
        matchAvailability.forEach(a => {
          availabilityMap[a.player_name as PlayerName] = a.status as AvailabilityStatus
        })
        return { ...match, availability: availabilityMap }
      })

      return <AppClient initialMatches={matchesWithAvailability} />
    }
    ```

    Maak `dekei1/src/components/AppClient.tsx` als nieuwe Client Component:
    ```typescript
    'use client'
    import { useState, useEffect } from 'react'
    import type { MatchWithAvailability, PlayerName } from '@/types'
    import { PlayerSelector } from './PlayerSelector'
    import { MatchCard } from './MatchCard'
    import Image from 'next/image'

    interface AppClientProps {
      initialMatches: MatchWithAvailability[]
    }

    export function AppClient({ initialMatches }: AppClientProps) {
      const [selectedPlayer, setSelectedPlayer] = useState<PlayerName | null>(null)

      return (
        <div style={{ backgroundColor: '#f5f5f5', minHeight: '100vh' }}>
          {/* Header */}
          <header style={{ backgroundColor: '#1B3A5C' }} className="px-4 py-5 text-white relative overflow-hidden">
            <div className="flex items-center gap-3 relative z-10">
              {/* Logo of tekst-logo */}
              <div className="flex-1">
                <h1 className="text-xl font-bold tracking-tight">DE KEI 1</h1>
                <p style={{ color: '#E87722' }} className="text-sm font-medium">Beschikbaarheid 2026</p>
              </div>
              {/* Tennisbal decoratie SVG */}
              <svg width="48" height="48" viewBox="0 0 48 48" className="opacity-20" aria-hidden="true">
                <circle cx="24" cy="24" r="20" fill="none" stroke="white" strokeWidth="3"/>
                <path d="M4 24 Q14 14 24 24 Q34 34 44 24" fill="none" stroke="white" strokeWidth="2"/>
                <path d="M4 24 Q14 34 24 24 Q34 14 44 24" fill="none" stroke="white" strokeWidth="2"/>
              </svg>
            </div>
          </header>

          {/* Speler selector */}
          <PlayerSelector selectedPlayer={selectedPlayer} onPlayerSelect={setSelectedPlayer} />

          {/* Wedstrijden */}
          <main className="max-w-lg mx-auto px-3 py-4 pb-8">
            {!selectedPlayer && (
              <p className="text-center text-sm text-gray-500 mb-4 py-2 px-4 bg-white rounded-lg border border-dashed border-gray-300">
                Kies je naam om je beschikbaarheid in te vullen
              </p>
            )}
            <div className="space-y-4">
              {initialMatches.map(match => (
                <MatchCard key={match.id} match={match} selectedPlayer={selectedPlayer} />
              ))}
            </div>
          </main>
        </div>
      )
    }
    ```

    Let op: `AppClient.tsx` toevoegen aan files_modified is OK — het is een nieuw bestand.

    Verwijder de inline card rendering uit Plan 01's page.tsx volledig — dit wordt nu gedaan door MatchCard component.

    De data wordt ALLEEN bij page load geladen (Server Component). Status updates via de API route gaan naar Supabase. Bij pagina refresh ziet de speler de bijgewerkte status van iedereen. Realtime updates (subscriptions) zijn NIET nodig voor v1 — de simpelheid is waardevol.
  </action>
  <verify>
    `cd /Users/michelhelsdingen/Documents/dekei1/dekei1 && npm run build 2>&1 | tail -20` — build slaagt.
    `npm run dev` starten, http://localhost:3000:
    - Donkerblauwe header met tennisbal SVG
    - PlayerSelector zichtbaar: 6 naam-knoppen
    - 6 wedstrijd cards zichtbaar
    - Klik op eigen naam → naam-knop wordt actief blauw
    - Klik Ja bij Ronde 1 → status wordt opgeslagen (controleer: bij pagina refresh staat status nog)
    - Klik op andermans wedstrijd rij → status knoppen disabled, geen effect
  </verify>
  <done>
    Build slaagt. Volledige app werkt:
    - Speler selecteert zichzelf → eigen rij is interactief
    - Klik Ja/Nee/Reserve → direct opgeslagen naar Supabase
    - Visuele bevestiging bij opslaan
    - Andermans rijen tonen status readonly (geen edit mogelijk)
    - Teller "X van 6 beschikbaar" correct
    - Bij pagina refresh: status van alle spelers nog zichtbaar
  </done>
</task>

</tasks>

<verification>
Na alle taken:
1. `cd /Users/michelhelsdingen/Documents/dekei1/dekei1 && npm run build` — slaagt zonder errors of warnings
2. `npx tsc --noEmit` — 0 TypeScript errors
3. Manuele check met `npm run dev`:
   - Selecteer naam "Michel" → Michel's rijen worden interactief, anderen readonly
   - Klik "Ja" bij Ronde 1 → opslaan, visuele bevestiging
   - Pagina refresh → status van Michel staat nog op "Ja" voor Ronde 1
   - Selecteer naam "Mark" → Marks rijen interactief, Michels status zichtbaar als readonly groen "Ja"
   - Teller toont correct aantal beschikbare spelers
4. Controleer Supabase: `curl -H "apikey: [anon_key]" "https://supabase.helsdingen.com/rest/v1/dekei1_availability"` — rijen aanwezig
</verification>

<success_criteria>
- Speler selectie werkt — klik naam → eigen rij actief (UX-02 bescherming)
- Ja/Nee/Reserve klik → direct opgeslagen, geen submit knop (AVAIL-01, AVAIL-02, BACK-02)
- Fancy icons bij status: ✓ groen, ✕ rood, ⏳ amber — duidelijk en mooi (AVAIL-03, UX-03)
- Teller "X van 6 beschikbaar" per wedstrijd correct en live (AVAIL-04)
- Andermans status knoppen volledig disabled (UX-02)
- Build en TypeScript checks slagen
- Refresh behoudt alle statussen
</success_criteria>

<output>
Na completion, maak `.planning/phases/02-app/02-02-SUMMARY.md`
</output>
